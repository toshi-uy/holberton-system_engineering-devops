#!/usr/bin/env bash
#Bash script that displays Holberton School 10 times using while.

x=0
while [ $x -le 9 ]
do
  echo "Holberton School"
  if [ $x -eq 3 ]
  then
    echo "bad luck"
  elif [ $x -eq 7 ]
  then
    echo "bad luck"
  else
    x=$(( x + 1 ))
    continue
  fi
  x=$(( x + 1 ))
done
